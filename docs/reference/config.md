# `todo` configuration

By default, `todo` expects its configuration to live in a `.todo.yml` file in
the project's root.

```yaml
tasks-directory: tasks
meta-directory: .todo
stages:
  - todo
  - work-in-progress
  - done
```
